# github credentials
githubuser:
githubpassword:

# git branches
branches: f20 f21 master el6

# prefix for all paths to local repository, %pkg replaced by package name
repo_path_prefix: /home/jchaloup/Packages/%pkg/upstream/
